local:
  enabled: true

secrets:
  jwtSecretKey: jwt-secret-key #checkov:skip=CKV_SECRET_6
  dbUser: movie-rating
  dbPassword: movie-rating-password #checkov:skip=CKV_SECRET_6
  dbDatabase: movie-rating
  dbAddress: movie-rating-postgresql
  dbPort: 5432
  otlpEndpoint: http://otel-collector.observability.svc.cluster.local:4317

postgresql:
  image:
    repository: bitnamilegacy/postgresql
  auth:
    username: movie-rating
    password: movie-rating-password #checkov:skip=CKV_SECRET_6
    database: movie-rating
  primary:
    networkPolicy:
      enabled: false

app:
  image:
    tag: "1.0.0"
  resources:
    requests:
      cpu: 15m
      memory: 225M
      ephemeral-storage: 128Mi
    limits:
      cpu: 29m
      memory: 598M
      ephemeral-storage: 256Mi
  ingress:
    enabled: true
    className: kong
    host: movie-rating.local.com

migrations:
  image:
    tag: "1.0.0"
  resources:
    requests:
      cpu: 15m
      memory: 225M
      ephemeral-storage: 128Mi
    limits:
      cpu: 29m
      memory: 598M
      ephemeral-storage: 2Gi
